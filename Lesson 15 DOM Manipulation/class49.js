// Create An Element

let h1 = document.createElement("h1"); // Create a tag 
h1.innerHTML = "Hello World"; // Content adding in h1 tag
h1.style.color = "red"; // style property in js
document.body.appendChild(h1); // h1 tag is add html > body

// console.log(h1);